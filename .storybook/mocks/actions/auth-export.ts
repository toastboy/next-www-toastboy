/**
 * Storybook stub for exporting auth data; avoids invoking server-only code during builds.
 * @returns Placeholder undefined result to satisfy action signature.
 */
export async function authExport() {
    return undefined;
}

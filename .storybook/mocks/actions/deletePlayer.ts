/**
 * Storybook stub for deleting a player; ensures UI stories do not mutate real data.
 * @returns Promise resolving to undefined to mirror the real action contract.
 */
export async function deletePlayer() {
    return undefined;
}

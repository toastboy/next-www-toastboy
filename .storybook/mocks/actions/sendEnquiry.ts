/**
 * Storybook stub for contact enquiry submission; keeps backend out of story builds.
 * @returns Promise resolving to undefined consistent with production implementation.
 */
export async function sendEnquiry() {
    return undefined;
}

/**
 * Storybook stub for submitting game invitation responses without hitting backend.
 * @returns Promise resolving to undefined for compatibility with production action.
 */
export async function submitGameInvitationResponse() {
    return undefined;
}

/**
 * Storybook stub for triggering invitations; keeps server logic out of Storybook bundles.
 * @returns Promise resolving to undefined matching the real action signature.
 */
export async function triggerInvitations() {
    return undefined;
}

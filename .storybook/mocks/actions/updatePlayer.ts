/**
 * Storybook stub for updating a player profile; avoids server writes during previews.
 * @returns Promise resolving to undefined consistent with production behaviour.
 */
export async function updatePlayer() {
    return undefined;
}

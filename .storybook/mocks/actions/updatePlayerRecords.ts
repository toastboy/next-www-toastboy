/**
 * Storybook stub for updating player records; prevents database mutations in story renders.
 * @returns Promise resolving to undefined to align with server action signature.
 */
export async function updatePlayerRecords() {
    return undefined;
}

Two issues here: (1) The next/headers mock returns a Promise (mockResolvedValue), which doesn’t match the real synchronous API—prefer mockReturnValue(new Headers()). (2) The test expectation keeps whitespace/casing in userEmails but expects normalization in userIdByEmail (later). That inconsistency can make behavior unclear and brittle; consider asserting a consistently normalized/trimmed userEmails list (or explicitly documenting why raw formatting is preserved).
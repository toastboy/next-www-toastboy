Hiding overlays via opacity: 0 keeps them present for hit-testing and accessibility; they can still intercept pointer events and may be exposed to assistive tech even when visually hidden. Consider also disabling interaction when hidden (e.g., pointerEvents: 'none', visibility: 'hidden', and/or aria-hidden while not ready), or conditionally render the overlays only after mugshotReady.
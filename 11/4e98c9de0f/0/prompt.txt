Now the user.playerId field is defined by the createAuthClient call in auth.client.ts so I'm not sure if it's possible to make that a full-blown @relation. It's created by Better Auth on the fly as I understand it.
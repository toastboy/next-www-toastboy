The production change is specifically about the gd.hallCost ?? 0 fallback, but this test only covers hallCost: 0 (which doesn’t exercise the nullish-coalescing path). Add a test case where hallCost is null/undefined (whichever is valid for your gameDays type) to ensure the runtime fallback is actually covered.
pct can become Infinity/NaN if progress[1] is 0, and it can exceed 100 if progress[0] > progress[1]. RingProgress sections generally expect values in the 0–100 range. Consider guarding against a zero total and clamping pct between 0 and 100 (and optionally handling negative values) before passing it to RingProgress.

---

The button can be clicked repeatedly while the async update is in-flight, potentially starting multiple concurrent updates. Consider tracking a local 'loading' state (disable the Button / show loading) until the promise settles, and optionally de-bounce clicks to prevent duplicate submissions.

---

String(error) often becomes '[object Object]' and may expose internal details to users. Prefer a user-safe message (similar to toPublicMessage usage elsewhere) and log/capture the raw error separately for diagnostics.

---

The export button can be clicked repeatedly while an export is already running, creating concurrent exports and multiple in-progress notifications. Consider adding local exporting state (like updating in AdminUpdatePlayerRecords) and wiring it to loading/disabled, or otherwise de-duping clicks until the current export finishes.
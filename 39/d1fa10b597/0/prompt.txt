The Outcome service mock is missing several methods that are used in the newly added page tests (e.g. getTurnoutByYear, getAdminByGameDay, getHistoryByPlayer). As written, those tests will throw when trying to call or configure a missing mock function (e.g. outcomeService.getTurnoutByYear is undefined). Add vi.fn() stubs for every Outcome method exercised by the tests so the suites don’t fail with 'undefined is not a function'.
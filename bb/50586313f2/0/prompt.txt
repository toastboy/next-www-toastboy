This method appears to be used to supply email details, but it returns a full-ish PlayerDataDisplayType by synthesising unrelated stats fields with null/0. That can be surprising for consumers and risks accidental display/logic errors if the object is reused elsewhere. Consider introducing a narrower DTO/type for email functionality (e.g. PlayerEmailDataDisplayType containing name + accountEmail + extraEmails) and update EmailPlayerButton/PlayerProfile to depend on that instead of a broader stats-bearing type.
The startsWith('2027-07-31') === false assertion doesn’t meaningfully validate the rollover logic (it only fails if the last row is exactly '2027-07-31'). Consider replacing it with an assertion that the computed booking-year end is in 2027 (e.g., verify at least one generated row is in 2027, or that the final row’s year is 2027, while still staying <= 2027-07-31).
This is a client component ('use client') but it imports a type from @/services/Player. Service modules in this codebase typically include import 'server-only';, which will break client bundling if imported at runtime. Make this a type-only import (import type { PlayerEmailDataDisplayType } ...) or move PlayerEmailDataDisplayType into src/types so the client never depends on server-only modules.
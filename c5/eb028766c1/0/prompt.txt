Previously this block explicitly normalized accountEmail to null (?? null). With the change, accountEmail now relies on whatever was selected into player. If the Prisma select doesn’t include accountEmail (or it’s optional in the inferred type), the returned object can have accountEmail: undefined, which violates PlayerDataType (string | null) and can cause inconsistent behavior downstream. Ensure the query selects accountEmail and/or set accountEmail: player.accountEmail ?? null in the returned object.
<task-notification>
<task-id>a35f3f28ea54dc5f3</task-id>
<tool-use-id>toolu_015JWA68qW1zPSSTth4g4Em6</tool-use-id>
<output-file>/private/tmp/claude-501/-Volumes-Development-next-www-toastboy/1a8073c5-beab-481d-b94e-e6e4a9b696ae/tasks/a35f3f28ea54dc5f3.output</output-file>
<status>completed</status>
<summary>Agent "Audit tests for leaking Mantine transition timers and Select usage" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>---

**Task 1 — At-risk test files**

All six have loading buttons that cycle true→false through an async handler, none have `vi.useFakeTimers`:

| File | Risk |
|------|------|
| `src/components/SignIn/SignIn.vitest.spec.tsx` | `loading={loading}` button resets after `signIn.email` resolves/rejects |
| `src/components/ClaimSignup/ClaimSignup.vitest.spec.tsx` | `loading={loading}` button resets after `signUp.email` resolves/rejects |
| `src/components/ResponsesForm/ResponsesForm.vitest.spec.tsx` | Per-row `loading={savingId===…}` + Mantine `&lt;Select&gt;` combobox opened/closed via `user.click` — **double exposure** (button transition + popover exit transition) |
| `src/components/PickerForm/PickerForm.vitest.spec.tsx` | Two loading buttons (`isSubmitting`, `isSettingEnabled`), both cycled in tests |
| `src/components/DrinkersForm/DrinkersForm.vitest.spec.tsx` | `loading={isSaving}` button resets after `setDrinkers` resolves |
| `src/components/MoneyForm/MoneyForm.vitest.spec.tsx` | Per-row `loading={submittingPlayerId===…}` resets after `payDebt` resolves |

The same `vi.useFakeTimers({ shouldAdvanceTime: true })` / `vi.clearAllTimers()` / `vi.useRealTimers()` pattern should be applied to all six. No `advanceTimers` option needed on `userEvent.setup()`.

---

**Task 2 — `Select` vs `NativeSelect` in `GameResultForm`**

**Only one component in the entire codebase uses Mantine `Select`:** `ResponsesForm`. `GameResultForm` uses `NativeSelect`; `NewPlayerForm` and `GameInvitationResponseForm` also use `NativeSelect`.

**Switching `GameResultForm` from `NativeSelect` to `Select` is safe.** The fake timer pattern already in place (`vi.useFakeTimers({ shouldAdvanceTime: true })` + `vi.clearAllTimers()` in `afterEach`) handles both button-loading transitions and `Select`/`Popover` exit timers — the `clearAllTimers()` call cancels whatever is pending regardless of source. The test currently uses `userEvent.selectOptions()` which only works on native `&lt;select&gt;` elements; after switching to Mantine `Select` those calls would need to change to `user.click(combobox)` + `user.click(option)` (same pattern `ResponsesForm`'s test already uses).

`ResponsesForm.vitest.spec.tsx` is in the at-risk list above and is the one other test interacting with Mantine `Select` — it also needs the fake timer treatment.</result>
<usage><subagent_tokens>122714</subagent_tokens><tool_uses>20</tool_uses><duration_ms>146607</duration_ms></usage>
</task-notification>
The nested extraEmails selection does not constrain returned fields, so Prisma will fetch all columns (e.g., ids, timestamps) even though the method only returns { email, verified }. Consider adding a nested select (e.g., select: { email: true, verifiedAt: true }) to minimize data exposure and reduce payload size.
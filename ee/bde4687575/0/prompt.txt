The React root created by createRoot(container) is never unmounted. That can leak effects between tests and sometimes triggers warnings. Store the root in a variable and call root.unmount() in afterEach before removing the container.
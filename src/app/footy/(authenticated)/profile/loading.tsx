import { PlayerProfileFormSkeleton } from '@/components/PlayerProfileForm/PlayerProfileFormSkeleton';

/** Skeleton placeholder matching the PlayerProfileForm layout (text inputs + switches + selects + button). */
const Loading = () => (
    <PlayerProfileFormSkeleton />
);

export default Loading;

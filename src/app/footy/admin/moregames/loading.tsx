import { MoreGamesFormSkeleton } from '@/components/MoreGamesForm/MoreGamesFormSkeleton';

/** Skeleton placeholder matching the MoreGamesForm layout (cost inputs + game table). */
const Loading = () => (
    <MoreGamesFormSkeleton />
);

export default Loading;

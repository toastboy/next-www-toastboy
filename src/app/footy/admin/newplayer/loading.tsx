import { NewPlayerFormSkeleton } from '@/components/NewPlayerForm/NewPlayerFormSkeleton';

/** Skeleton placeholder matching the NewPlayerForm layout (name + email + select + button). */
const Loading = () => (
    <NewPlayerFormSkeleton />
);

export default Loading;

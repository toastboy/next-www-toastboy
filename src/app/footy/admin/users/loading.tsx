import { AdminUserListSkeleton } from '@/components/AdminUserList/AdminUserListSkeleton';

/** Skeleton placeholder matching the AdminUserList layout (search + sortable user table). */
const Loading = () => (
    <AdminUserListSkeleton />
);

export default Loading;

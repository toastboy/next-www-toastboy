import { vi } from 'vitest';

vi.mock('services/CountrySupporter');

describe('CountryMap page', () => {
    it.todo('calls countrySupporterService.getAllWithCountry');
    it.todo('passes the countries data to the PlayerCountryMap component');
    it.todo('handles service errors gracefully');
});

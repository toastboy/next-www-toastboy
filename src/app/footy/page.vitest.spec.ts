// This page is purely static (renders a crest image with no data fetching or
// logic). Meaningful coverage lives in Storybook.
describe('Footy home page', () => {
    it.todo('renders the Toastboy FC crest');
});

import { vi } from 'vitest';

vi.mock('next/navigation');

describe('Player [id]/[year] redirect page', () => {
    it.todo('calls permanentRedirect to /footy/player/[id]?year=[year]');
    it.todo('preserves the exact id and year values from params');
});

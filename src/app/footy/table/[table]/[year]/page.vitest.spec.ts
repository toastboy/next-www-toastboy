import { vi } from 'vitest';

vi.mock('next/navigation');

describe('Table [table]/[year] redirect page', () => {
    it.todo('calls permanentRedirect to /footy/table/[table]?year=[year]');
    it.todo('preserves the exact table and year values from params');
});

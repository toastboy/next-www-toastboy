import { vi } from 'vitest';

vi.mock('services/Outcome');

describe('Turnout page', () => {
    it.todo('calls outcomeService.getTurnoutByYear');
    it.todo('passes the turnout data to the Turnout component');
    it.todo('handles service errors gracefully');
});

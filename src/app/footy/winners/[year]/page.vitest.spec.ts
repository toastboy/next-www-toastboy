import { vi } from 'vitest';

vi.mock('next/navigation');

describe('Winners [year] redirect page', () => {
    it.todo('calls permanentRedirect to /footy/winners?year=[year]');
    it.todo('preserves the exact year value from params');
});

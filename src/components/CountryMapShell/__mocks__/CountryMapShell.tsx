import type { Props } from '../CountryMapShell';

export const CountryMapShell = (props: Props) => (
    <div>CountryMapShell: {JSON.stringify(props)}</div>
);
CountryMapShell.displayName = 'CountryMapShell';

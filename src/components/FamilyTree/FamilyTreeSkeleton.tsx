import { Skeleton } from '@mantine/core';

/** Skeleton placeholder matching the FamilyTree component layout. */
export const FamilyTreeSkeleton = () => (
    <Skeleton data-testid="skeleton-family-tree" height={928} radius="md" />
);

import { Props } from '../PlayerProfileForm';

export const PlayerProfileForm = (props: Props) => (
    <div>PlayerProfileForm: {JSON.stringify(props)}</div>
);
PlayerProfileForm.displayName = 'PlayerProfileForm';

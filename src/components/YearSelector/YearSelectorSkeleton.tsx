import { Skeleton } from '@mantine/core';

/** Skeleton placeholder matching the YearSelector component layout. */
export const YearSelectorSkeleton = () => (
    <Skeleton data-testid="skeleton-year-selector" height={86} width="100%" />
);

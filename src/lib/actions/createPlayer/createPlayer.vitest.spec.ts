import { describe, it } from 'vitest';


describe('createPlayerCore', () => {
    it.todo('creates a player and returns an invite link');
});

describe('addPlayerInviteCore', () => {
    it.todo('creates an email verification and returns a claim URL');
});

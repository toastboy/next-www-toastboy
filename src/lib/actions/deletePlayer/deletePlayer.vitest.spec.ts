import { describe, it } from 'vitest';


describe('beforeDeletePlayerCore', () => {
    it.todo('cleans up player data before deletion');
});

describe('deletePlayerCore', () => {
    it.todo('deletes the authenticated user');
});
